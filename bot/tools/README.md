# Local voice tools

Place a Windows FFmpeg binary at `ffmpeg/ffmpeg.exe`, or set `FFMPEG_BIN` to its absolute path.

The binary is intentionally ignored by Git. Install it separately on the bot server.
